# Model

Software reference models for the accelerator live here.

The Python model should be treated as the golden functional reference for RTL verification. Keep arithmetic behavior explicit: signed widths, accumulation width, requantization, rounding, saturation, and activation behavior should all match the hardware specification.
